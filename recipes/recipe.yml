---
# yaml-language-server: $schema=https://schema.blue-build.org/recipe-v1.json
# image will be published to ghcr.io/<user>/<name>
name: hoxi-kinoite
# description will be included in the image's metadata
description: This is my personal OS image.

# the base image to build on top of (FROM) and the version tag to use
base-image: ghcr.io/blue-build/base-images/fedora-kinoite
image-version: 44 # latest is also supported if you want new updates ASAP

# module configuration, executed in order
# you can include multiple instances of the same module
modules:
  - type: files
    files:
      - source: system
        destination: / # copies files/system/* (* means everything inside it) into your image's root folder /

  - type: dnf
    repos:
      copr:
        - atim/starship
    install:
      packages:
        - micro
        - starship
    remove:
      packages:
        # example: removing firefox (in favor of the flatpak)
        # "firefox" is the main package, "firefox-langpacks" is a dependency
        - firefox
        - firefox-langpacks # also remove firefox dependency (not required for all packages, this is a special case)

  - type: default-flatpaks
    configurations:
      - notify: true # Send notification after install/uninstall is finished (true/false)
        scope: system
        # If no repo information is specified, Flathub will be used by default
        install: # system flatpaks we want all users to have and not remove
          - org.mozilla.firefox
          - org.gnome.Loupe
      - scope: user # Also add Flathub user repo, but no user packages

  - type: copy
    from: ghcr.io/ublue-os/akmods:ogc-44
    src: /kernel-rpms
    dest: /tmp/kernel-rpms

  # Pull in the matching Nvidia kmod + driver RPMs (and ublue's install script)
  - type: copy
    from: ghcr.io/ublue-os/akmods-nvidia-open:ogc-44
    src: /rpms
    dest: /tmp/akmods-rpms

  - type: script
    snippets:
      # Swap the default kernel for kernel-ogc
      - dnf -y remove kernel-* && rm -drf /usr/lib/modules/*
      - dnf -y install --setopt=install_weak_deps=False /tmp/kernel-rpms/kernel-*.rpm
      - rm -r /tmp/kernel-rpms

      # Run ublue's own nvidia install script against the rpms we just copied in
      - chmod +x /tmp/akmods-rpms/ublue-os/nvidia-install.sh
      - AKMODNV_PATH=/tmp/akmods-rpms IMAGE_NAME=kinoite /tmp/akmods-rpms/ublue-os/nvidia-install.sh
      - rm -rf /tmp/akmods-rpms

  - type: kargs
    kargs:
      - rd.driver.blacklist=nouveau
      - modprobe.blacklist=nouveau
      - nvidia-drm.modeset=1

  - type: signing # this sets up the proper policy & signing files for signed images to work fully
